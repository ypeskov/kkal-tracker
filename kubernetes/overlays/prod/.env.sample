#.env.prod.sample

# Server port
SERVER_PORT=8080

# Path to the SQLite database
DATABASE_PATH=/data/app.db

# JWT secret key
JWT_SECRET=a-very-secret-key

# Google Drive backup configuration
# Full content of the service account JSON file (budgeter-448210-8f3ac0986830.json)
# Copy the entire JSON content and paste it here as a single line
GOOGLE_CREDENTIALS_JSON='{"type":"service_account","project_id":"budgeter-448210",...}'

# Google Drive folder path for backups
GDRIVE_FOLDER_PATH="/services/kkal-tracker/backups"
